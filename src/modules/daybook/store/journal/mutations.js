// export const myMutation = (state) => { return state.something; } MODIFICAR STATE "REACTIVIDAD"
export const setEntries = (state) => {
    return state;
}
export const updateEntry = (state) => {
    return state;
}
export const createEntry = (state) => {
    return state;
}
export const removeEntry = (state) => {
    return state;
}